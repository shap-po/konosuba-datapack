# continue with the revive if there is a target
execute if score @s konosuba.lives.select1 matches 0.. run function konosuba:origins/aqua/revive/revive_1

function konosuba:origins/aqua/revive/cleanup
